# ===========================================================================
# VolunteerArmyPC —— 入库排除规则
#
# 原则：源码与工程配置全入库；构建产物、第三方编辑器本体、开发期取证截图
#       全部排除。
#
# 注意本文件里 "目录/*" + "!目录/某文件" 的写法是**故意**的：
#   - 不能写成 "目录/"：父目录本身被排除后，子文件的 "!" 反选会失效
#     （gitignore 规则：父目录已被排除时，无法再反选其中的文件）。
#   - 反选出来的 .gdignore 是**必需**的：它让 Godot 不去扫描 sdk/ 与 build/，
#     否则首次打开项目时 Godot 会把 build/ 里 1000+ 个 .obj 当 3D 模型导入，
#     卡死几分钟并污染 .godot/ 缓存。
# ===========================================================================

# --- 构建产物（每次构建都会变，不入库；请照 README 自行构建）---
/build/*
!/build/.gdignore
/bin/

# --- Godot 编辑器本体（约 229MB）与导入缓存 ---
/sdk/*
!/sdk/.gdignore
/.godot/

# --- Godot 的导入产物（与 .godot/ 同理：源码入库，导入产物不入库）---
# 每次启动项目、或跑一次 `godot --headless --import`，Godot 都会给每个被导入的
# 资源在**源码旁边**写一个 `<文件>.import`。它只记录"用什么参数导入 + 产物校验和"，
# 删掉不会有任何损失 —— 下次启动会照原样重新扫出来。
# 本仓库从第一天起就没入库过它们（历史提交里一个 .import 都没有），
# 但之前没写规则，于是跑一次 --import 就会在 git status 里冒出 100+ 个未跟踪文件，
# 把真正的改动（比如一个新模型）淹掉。这里把这条既有约定显式化。
*.import
# 附带一个转瞬即逝的中间文件（导入进行中才存在，名字形如 xxx.import123456.tmp）
*.import[0-9]*.tmp

# gltf 导入默认会把 **GLB 内嵌的贴图抽成同级 jpg**
# （gltf/embedded_image_handling 默认为 Extract，本项目没改过），
# 于是 model/ 下每个 .glb 旁边会长出 3 个 jpg（基础色 / 法线 / 金属-粗糙）。
# 它们与 .glb 内容重复，且同样可以重新生成 —— 入库只会让 11 个模型
# 平白多出 33 个重复贴图文件。
/assets/art/char/model/*_texture_pbr_*.jpg

# --- 开发期取证截图与运行日志（体积大，随时可用 tools/capture.sh 重新生成）---
# 写成 "/sweep/*" + 反选而不是 "/sweep/"：目录本身若被排除，里面的
# .gdignore 就永远反选不回来（gitignore 的父目录优先规则）。
/sweep/*
!/sweep/.gdignore
/captures/*
!/captures/.gdignore
*.log
# --- 导出的可执行程序（约 165 MB，随时可用 README 的导出命令重新生成）---
/dist/
# tools/capture_ui.sh 会把五屏取证的日志按屏分开写（run_ui_ui3.log.menu 这种
# 名字不匹配上面的 *.log，所以单独补一条；只放行 assets 下的正式素材）
run_ui_*.log.*

# --- 第三方参考图（版权不在本项目，且本仓库是 Public）---
# ref/ 里是调研阶段从各壁纸站下载的《使命召唤》官方宣传图，只用于提炼
# 视觉语法（冷底 + 单一暖锚点 + 人物剪影 + 轮廓光）。这些图的版权属于
# 原发行商，**不能**随本仓库分发；需要时按 README 的说明自行下载。
/ref/

# --- 角色立绘的未处理原始版（22MB）---
# assets/art/char/raw/ 是生成器直接吐出来的原图：带半透明水印、尺寸不统一。
# 它们**不是**任何用途的输入 —— 图生3D 与胸像裁切都吃同级目录里
# 由 tools/prep_char.py 处理过的 <键>.png（去水印 + 规范到 1024×1469）。
# 也就是说 raw/ 被处理产物完全覆盖，留着只是占地方，且带着水印的图
# 在 Public 仓库里容易让人误以为是正式素材。
/assets/art/char/raw/

# --- 本地 AI 会话记忆 ---
/.workbuddy/

# --- 编辑器 / 系统 ---
.vscode/
.idea/
Thumbs.db
desktop.ini
.DS_Store

# --- Python 字节码缓存（tools/ 下的纯 Python 工具跑一次就会生成）---
__pycache__/
*.pyc
